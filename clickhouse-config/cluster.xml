<clickhouse>
    <!-- Built-in Keeper (ZooKeeper replacement) for single-node ClickHouse cluster -->
    <keeper_server>
        <tcp_port>9181</tcp_port>
        <server_id>1</server_id>
        <log_storage_path>/var/lib/clickhouse/coordination/log</log_storage_path>
        <snapshot_storage_path>/var/lib/clickhouse/coordination/snapshots</snapshot_storage_path>
        <raft_configuration>
            <server>
                <id>1</id>
                <hostname>localhost</hostname>
                <port>9234</port>
            </server>
        </raft_configuration>
    </keeper_server>

    <!-- Point ZooKeeper client at the built-in Keeper -->
    <zookeeper>
        <node>
            <host>localhost</host>
            <port>9181</port>
        </node>
    </zookeeper>

    <!-- Single-node cluster named "posthog" — matches PostHog's CLICKHOUSE_CLUSTER default -->
    <remote_servers>
        <posthog>
            <shard>
                <replica>
                    <host>posthog-clickhouse</host>
                    <port>9000</port>
                </replica>
            </shard>
        </posthog>
    </remote_servers>

    <macros>
        <shard>01</shard>
        <replica>01</replica>
    </macros>
</clickhouse>
